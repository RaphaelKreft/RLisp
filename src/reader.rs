/*
reader.rs:  Holds the parser for S-Expressions, that is used to build up the sytaxtree
            Datastructure from input. The resulting Datastrucure is later used of eval.
*/

pub fn read() {
    println!("Parse");
}